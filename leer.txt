Como se puede ver en la captura de pantalla, funciona.
Utilizo REST Client en VS Studio para conectarme al servidor NEST en Docker y que devuelva el HelloWorld por defecto.
También se muestra como funciona el pgAdmin conectado a la base de datos dtgIncidencias
Por último, se puede ver en la terminal en remoto por SSH a la máquina virtual con el Docker mostrando el resultado de un docker logs al contenedor del servidor NEST y todo funciona bien.
Si no hago más pruebas lanzando api request es porque el proyecto está praacticamente vacío, por eso he probado con la api request básica que resuelve un HelloWorld para comprobar y demostrar que funciona y resuelve las api request